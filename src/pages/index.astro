---
import { negotiateLocale } from '@/i18n/negotiate'

// Jazyk podle Accept-Language — stejná logika jako u zkratek /go/<klíč>.
return Astro.redirect(`/${negotiateLocale(Astro.request.headers.get('accept-language'))}`)
---
