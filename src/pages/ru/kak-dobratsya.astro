---
import Base from '@/layouts/Base.astro'
import FaqSchema from '@/components/FaqSchema.astro'
import PillarPage from '@/components/PillarPage.astro'
import MarkdocRenderer from '@/components/MarkdocRenderer.astro'
import DestinationMap from '@/components/DestinationMap.astro'
import { getPage } from '@/content/reader'

const locale = 'ru' as const
const page = await getPage('ru-kak-dobratsya')
---

<Base locale={locale} section="practical-info" title={page?.metaTitle ?? ""} description={page?.metaDescription ?? ""} alternateUrls={{ de: '/de/anreise', en: '/en/getting-there', cs: '/cs/jak-se-dostat', ru: '/ru/kak-dobratsya' }}>
  <FaqSchema rawBody={page?.rawBody} />
  <PillarPage locale={locale} title={page?.title ?? ""} description={page?.metaDescription ?? ""}>
    { page?.body && <MarkdocRenderer content={page.body} /> }
    <DestinationMap locale={locale} />
  </PillarPage>
</Base>
