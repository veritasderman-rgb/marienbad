---
import Base from '@/layouts/Base.astro'
import PillarPage from '@/components/PillarPage.astro'
import MarkdocRenderer from '@/components/MarkdocRenderer.astro'
import { getPage } from '@/content/reader'
import { t } from '@/i18n/ui'

const locale = 'ru' as const
const page = await getPage('ru-golf')
const breadcrumbs = [
  { label: t(locale, 'nav.home'), href: `/${locale}` },
  { label: t(locale, 'nav.thingsToDo'), href: `/${locale}/chem-zanyatsya` },
  { label: 'Гольф' },
]
---

<Base locale={locale} section="things-to-do" title={page?.metaTitle ?? ""} description={page?.metaDescription ?? ""} alternateUrls={{ de: '/de/golf', en: '/en/golf', cs: '/cs/golf', ru: '/ru/golf' }}>
  <PillarPage locale={locale} title={page?.title ?? ""} description={page?.metaDescription ?? ""} breadcrumbs={breadcrumbs}>
    { page?.body && <MarkdocRenderer content={page.body} /> }
  </PillarPage>
</Base>
