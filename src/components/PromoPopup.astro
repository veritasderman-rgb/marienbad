---
/**
 * PromoPopup — Family vacation discount campaign
 * Teaser: 11–14 May 2026 → hints at upcoming 15% discount
 * Active: 15 May 2026 (00:00–23:59) → shows code RODINA15
 * Hotels: Ensana Hvězda & Ensana Butterfly
 * Valid for stays until 31 Aug 2026
 */

const bookingUrl = 'https://ensanahotels.com/cs/hotely/hvezda/nabidky?utm_source=marienbad&utm_medium=popup&utm_campaign=rodina15'
---

<div
  id="promo-popup-overlay"
  class="fixed inset-0 z-[60] bg-black/50 backdrop-blur-sm flex items-center justify-center p-4 opacity-0 pointer-events-none transition-opacity duration-300"
  role="dialog"
  aria-modal="true"
  aria-label="Speciální nabídka"
>
  <!-- TEASER CARD (May 11–14) -->
  <div id="promo-teaser" class="hidden relative bg-white rounded-2xl shadow-2xl overflow-hidden max-w-md w-full transition-transform duration-300 scale-95">
    <div class="px-6 pt-8 pb-10 text-center text-white relative" style="background: linear-gradient(135deg, #004F71 0%, #3199b3 100%);">
      <button class="promo-close absolute top-3 right-3 w-8 h-8 flex items-center justify-center rounded-lg bg-white/15 hover:bg-white/25 transition-colors cursor-pointer" aria-label="Zavřít">
        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
      <p class="text-turquoise-200 text-xs font-semibold tracking-[0.2em] uppercase mb-3">Již 15. května</p>
      <h2 class="font-heading text-2xl md:text-3xl font-bold leading-tight">Rodinná dovolená<br/>se slevou 15 %</h2>
    </div>
    <div class="px-6 py-6 text-center">
      <p class="text-beige-700 text-base leading-relaxed mb-6">
        Chystáme pro vás speciální slevový kód na balíček Rodinná dovolená v hotelech Ensana Hvězda a Ensana Butterfly. Pobyt do 31. srpna — ideální na letní prázdniny s dětmi.
      </p>
      <div class="border-2 border-dashed border-turquoise-300 bg-beige-50 rounded-xl py-4 px-6 mb-5">
        <p class="text-turquoise-600 text-xs font-semibold tracking-[0.2em] uppercase mb-1">Odpočítávání</p>
        <p id="promo-countdown" class="font-heading text-2xl md:text-3xl font-bold text-indigo-700">...</p>
      </div>
      <a
        href={bookingUrl}
        target="_blank"
        rel="noopener noreferrer"
        class="inline-block w-full bg-yellow-400 hover:bg-yellow-500 text-indigo-900 font-bold py-3.5 px-6 rounded-full text-center transition-colors"
      >
        Zobrazit nabídky Ensana Hvězda
      </a>
      <p class="text-beige-500 text-xs mt-4">Hotely Hvězda a Butterfly &bull; Prázdniny 2026</p>
    </div>
  </div>

  <!-- ACTIVE CARD (May 15) -->
  <div id="promo-active" class="hidden relative bg-white rounded-2xl shadow-2xl overflow-hidden max-w-md w-full transition-transform duration-300 scale-95">
    <div class="px-6 pt-8 pb-10 text-center text-white relative" style="background: linear-gradient(135deg, #004F71 0%, #3199b3 100%);">
      <button class="promo-close absolute top-3 right-3 w-8 h-8 flex items-center justify-center rounded-lg bg-white/15 hover:bg-white/25 transition-colors cursor-pointer" aria-label="Zavřít">
        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
      <p class="text-turquoise-200 text-xs font-semibold tracking-[0.2em] uppercase mb-3">Jen 15. května</p>
      <h2 class="font-heading text-2xl md:text-3xl font-bold leading-tight">Rodinná dovolená<br/>se slevou 15 %</h2>
    </div>
    <div class="px-6 py-6 text-center">
      <p class="text-beige-700 text-base leading-relaxed mb-5">
        15. května celý den platí kód RODINA15 na balíček Rodinná dovolená v hotelech Hvězda a Butterfly. Pobyt si vyberte kdykoli do 31. srpna — ideální termín na letní prázdniny s dětmi.
      </p>
      <div class="border-2 border-dashed border-turquoise-300 bg-beige-50 rounded-xl py-4 px-6 mb-5">
        <p class="text-turquoise-600 text-xs font-semibold tracking-[0.2em] uppercase mb-1">Slevový kód</p>
        <p class="font-heading text-3xl md:text-4xl font-bold text-indigo-700 tracking-wider select-all">RODINA15</p>
      </div>
      <a
        href={bookingUrl}
        target="_blank"
        rel="noopener noreferrer"
        class="inline-block w-full bg-yellow-400 hover:bg-yellow-500 text-indigo-900 font-bold py-3.5 px-6 rounded-full text-center transition-colors"
      >
        Rezervovat pobyt
      </a>
      <p class="text-beige-500 text-xs mt-4">
        Platnost kódu: 15. 5. 2026, 0:00–23:59<br/>
        Pobyt do 31. 8. 2026 &bull; Hotely Hvězda a Butterfly
      </p>
    </div>
  </div>
</div>

<script is:inline>
  (function () {
    var overlay = document.getElementById('promo-popup-overlay');
    var teaser = document.getElementById('promo-teaser');
    var active = document.getElementById('promo-active');
    if (!overlay || !teaser || !active) return;

    var now = new Date();
    var y = now.getFullYear();
    var m = now.getMonth();
    var d = now.getDate();

    var phase = 'none';
    if (y === 2026 && m === 4) {
      if (d >= 11 && d <= 14) phase = 'teaser';
      else if (d === 15) phase = 'active';
    }
    if (phase === 'none') return;

    var storageKey = 'promo-rodina15-' + phase + '-dismissed';
    if (localStorage.getItem(storageKey)) return;

    // Suppress the regular BookingPopup
    sessionStorage.setItem('booking-popup-dismissed', '1');

    var card = phase === 'teaser' ? teaser : active;
    card.classList.remove('hidden');

    // Countdown for teaser phase
    if (phase === 'teaser') {
      var countdownEl = document.getElementById('promo-countdown');
      if (countdownEl) {
        var target = new Date(2026, 4, 15, 0, 0, 0);
        function updateCountdown() {
          var diff = target.getTime() - Date.now();
          if (diff <= 0) {
            countdownEl.textContent = 'Už je to tady!';
            return;
          }
          var days = Math.floor(diff / 86400000);
          var hours = Math.floor((diff % 86400000) / 3600000);
          var mins = Math.floor((diff % 3600000) / 60000);
          var secs = Math.floor((diff % 60000) / 1000);
          var parts = [];
          if (days > 0) parts.push(days + 'd');
          parts.push(hours + 'h');
          parts.push(mins + 'm');
          parts.push(secs + 's');
          countdownEl.textContent = parts.join(' ');
        }
        updateCountdown();
        setInterval(updateCountdown, 1000);
      }
    }

    function show() {
      overlay.classList.remove('opacity-0', 'pointer-events-none');
      overlay.classList.add('opacity-100', 'pointer-events-auto');
      card.classList.remove('scale-95');
      card.classList.add('scale-100');
    }

    function hide() {
      overlay.classList.add('opacity-0', 'pointer-events-none');
      overlay.classList.remove('opacity-100', 'pointer-events-auto');
      localStorage.setItem(storageKey, '1');
    }

    var closeButtons = card.querySelectorAll('.promo-close');
    for (var i = 0; i < closeButtons.length; i++) {
      closeButtons[i].addEventListener('click', hide);
    }

    overlay.addEventListener('click', function (e) {
      if (e.target === overlay) hide();
    });

    document.addEventListener('keydown', function (e) {
      if (e.key === 'Escape') hide();
    });

    setTimeout(show, 2500);
  })();
</script>
